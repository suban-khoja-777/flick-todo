const showSpinner = () =>  {
    document.querySelector('.app-spinner').classList.remove('hide');
}

const hideSpinner = () =>  {
    document.querySelector('.app-spinner').classList.add('hide');
}